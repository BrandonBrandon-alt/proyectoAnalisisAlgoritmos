COMPONENTES CONEXAS (formato legible)
======================================================================

Componente 1 (tamaño=30): adversarial → ai literacy → algorithmic bias → co creation → data → ethics → explainability → fine tuning → generative → generative models → human ai interaction → image → images → intelligence → learning → machine learning → method → methods → model → models → multimodality → network → paper → personalization → privacy → prompting → proposed → training data → transparency → using
